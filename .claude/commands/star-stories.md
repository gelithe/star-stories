---
description: Load the Star Stories project context (personalised children's books)
---

You are picking up **Star Stories** — personalised children's books generated
from a child's real birth chart.

Read these files, in this order, before doing anything else:

1. `HANDOFF.md` — current state, decisions already made, next build
2. `AGE-BANDS.md` — the translation craft: story-as-mirror ethic,
   the five-step method, the seven age bands, worked examples per band
3. `LANGUAGES.md` — the language system and per-age mixing shapes
4. `README.md` — product definition and project layout
5. `PRINT.md` and `ILLUSTRATIONS.md` — only if the task
   touches printing or artwork

Then summarise back in five lines what the project is and what state it is in,
and ask what we are building today.

Hold these while you work:

- **Story as mirror, never prediction as destiny.** A child inherits a poem, not
  a box. This is not negotiable.
- The three finished books in `books/` are the reference
  implementations. Study them before writing a new one.
- Work only inside ``.
- On prompts: fewer rules beat more. Prohibitions make the model write
  defensively; a clear persona and positively-stated principles make it write
  well. If a brief is growing longer, it is probably getting worse.
